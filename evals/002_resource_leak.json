{
  "name": "unclosed-file-resource-leak",
  "pr": {
    "title": "Add config reader",
    "body": "Reads a config file from disk and returns its bytes.",
    "files": [
      {
        "path": "config.go",
        "language": "go",
        "patch": "@@ -0,0 +1,8 @@\n+func ReadConfig(path string) ([]byte, error) {\n+\tf, err := os.Open(path)\n+\tif err != nil {\n+\t\treturn nil, err\n+\t}\n+\tdata, err := io.ReadAll(f)\n+\treturn data, err\n+}",
        "content": "package main\n\nimport (\n\t\"io\"\n\t\"os\"\n)\n\nfunc ReadConfig(path string) ([]byte, error) {\n\tf, err := os.Open(path)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tdata, err := io.ReadAll(f)\n\treturn data, err\n}\n"
      }
    ]
  },
  "expected": [
    { "file": "config.go", "line": 2, "keywords": ["not closed", "resource leak", "file leak", "defer f.close", "close the file", "leak"] }
  ]
}
